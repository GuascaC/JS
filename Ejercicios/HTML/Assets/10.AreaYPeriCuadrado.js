'use strict';
let lado = 0;
let area = 0;
let perimetro = 0;
lado=prompt("Ingrese el lado del cuadrado");
area = Math.pow(lado,2);
perimetro = parseFloat(lado) * 4;
alert(area);
alert(perimetro);
