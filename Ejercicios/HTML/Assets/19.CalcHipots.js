'use strict';
let cat1 = 0;
let cat2 = 0;
cat1=prompt("Ingrese el cateto 1: ");
cat2=prompt("Ingrese el cateto 2: ");
hipo =  Math.sqrt(Math.pow(cat1,2)+Math.pow(cat2,2)); 
console.log("La Hipotenusa es :"+hipo);
